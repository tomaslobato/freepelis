export type Movie = {
    id: string
    title: string
    image_url: string
    year: string
}

export type TMDBMovie = {
    id: string
    title: string
    poster_path : string
    release_date: string
}